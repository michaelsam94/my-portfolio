SECTIONS = {
  "variable-fonts-performance-ux": [('Why this breaks in production', ['We switched six static WOFF2 files to one variable Inter file and cut font payload from 420 KB to 180 KB after subsetting—LCP improved 180 ms on mobile.', '**When:** When variable fonts performance ux sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating variable fonts performance ux as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for variable fonts performance ux: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "vector-databases-in-production": [('Why this breaks in production', ['The team evaluated Pinecone and Qdrant, then asked: can we use Postgres? Eight hundred thousand chunks with permission joins made pgvector the right tool—not a compromise.', '**When:** When vector databases in production sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating vector databases in production as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for vector databases in production: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "vector-db-filtering-pre-post": [('Why this breaks in production', ['Our RAG pipeline returned wrong-tenant documentation for three weeks because post-filtering discarded global neighbors that failed tenant_id checks.', '**When:** When vector db filtering pre post sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating vector db filtering pre post as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for vector db filtering pre post: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "vector-db-pgvector-postgres": [('Why this breaks in production', ['Adding pgvector took an afternoon; operating a second vector database would have taken a quarter.', '**When:** When vector db pgvector postgres sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating vector db pgvector postgres as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for vector db pgvector postgres: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "vector-db-sharding-scaling": [('Why this breaks in production', ['At thirty million vectors our Qdrant node started swapping—sharding was survival, not optimization.', '**When:** When vector db sharding scaling sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating vector db sharding scaling as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for vector db sharding scaling: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "vector-search-hnsw-tuning": [('Why this breaks in production', ['We celebrated 12 ms p50 queries until recall@10 against brute force came back at 71%.', '**When:** When vector search hnsw tuning sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating vector search hnsw tuning as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for vector search hnsw tuning: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "vector-search-ivf-pq-index": [('Why this breaks in production', ['HNSW ate 38 GB RAM; IVF-PQ cut memory to 6 GB at 93% recall—good enough for recommendations, not for legal RAG.', '**When:** When vector search ivf pq index sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating vector search ivf pq index as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for vector search ivf pq index: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "vex-vulnerability-triage-sbom": [('Why this breaks in production', ['Our scanner reported 847 critical CVEs on one image; VEX assertions dropped actionable issues to 23.', '**When:** When vex vulnerability triage sbom sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating vex vulnerability triage sbom as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for vex vulnerability triage sbom: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "view-transitions-api": [('Why this breaks in production', ['We replaced a 14 KB transition library with forty lines of CSS and startViewTransition.', '**When:** When view transitions api sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating view transitions api as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for view transitions api: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "voice-agents-stt-tts-pipelines": [('Why this breaks in production', ['End-to-end voice latency was 2.4 seconds until we streamed STT, LLM tokens, and TTS audio chunks.', '**When:** When voice agents stt tts pipelines sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating voice agents stt tts pipelines as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for voice agents stt tts pipelines: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "vue-3-composition-api-patterns": [('Why this breaks in production', ['Three mixins had colliding data() keys until composables made dependencies explicit.', '**When:** When vue 3 composition api patterns sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating vue 3 composition api patterns as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for vue 3 composition api patterns: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "wasm-workloads-kubernetes": [('Why this breaks in production', ['Two thousand container cold starts per minute became WASM Spin pods at 35 ms median cold start.', '**When:** When wasm workloads kubernetes sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating wasm workloads kubernetes as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for wasm workloads kubernetes: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "wcag-22-new-criteria-implementation": [('Why this breaks in production', ['Forty-seven new violations appeared the week WCAG 2.2 became the procurement standard—mostly sticky headers and 20px icon buttons.', '**When:** When wcag 22 new criteria implementation sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating wcag 22 new criteria implementation as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for wcag 22 new criteria implementation: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-accessibility-aria-patterns": [('Why this breaks in production', ['Three hundred forty ARIA attributes made NVDA harder to use until we stripped redundant roles and fixed three composite widgets.', '**When:** When web accessibility aria patterns sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web accessibility aria patterns as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web accessibility aria patterns: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-accessibility-keyboard-navigation": [('Why this breaks in production', ['Tab order jumped through four hundred grid cells before the save button until we fixed roving tabindex on the data table.', '**When:** When web accessibility keyboard navigation sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web accessibility keyboard navigation as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web accessibility keyboard navigation: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-accessibility-screen-reader-testing": [('Why this breaks in production', ['Axe passed checkout; NVDA users got lost in the autocomplete until we tested with real screen readers.', '**When:** When web accessibility screen reader testing sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web accessibility screen reader testing as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web accessibility screen reader testing: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-color-functions-oklch": [('Why this breaks in production', ['HSL gray steps looked identical on one monitor and miles apart on another until we migrated neutrals to OKLCH.', '**When:** When web color functions oklch sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web color functions oklch as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web color functions oklch: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-components-2026": [('Why this breaks in production', ['One ds-button ships in React, Vue, and Angular apps because web components beat three framework copies.', '**When:** When web components 2026 sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web components 2026 as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web components 2026: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-components-form-association": [('Why this breaks in production', ['Hidden inputs syncing custom rating widgets broke on form reset until formAssociated custom elements shipped.', '**When:** When web components form association sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web components form association as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web components form association: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-components-shadow-dom": [('Why this breaks in production', ['Global button { border-radius: 0 } stopped breaking design system buttons inside shadow roots.', '**When:** When web components shadow dom sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web components shadow dom as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web components shadow dom: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-dialog-element-modal": [('Why this breaks in production', ['We deleted four hundred lines of focus-trap modal JavaScript and used showModal() instead.', '**When:** When web dialog element modal sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web dialog element modal as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web dialog element modal: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-forms-native-validation": [('Why this breaks in production', ['Removing Yup from signup improved conversion when native constraint validation showed errors immediately.', '**When:** When web forms native validation sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web forms native validation as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web forms native validation: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-htmx-server-driven-ui": [('Why this breaks in production', ['One hundred eighty KB of React admin JS became htmx partial swaps on server-rendered tables.', '**When:** When web htmx server driven ui sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web htmx server driven ui as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web htmx server driven ui: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-islands-partial-hydration": [('Why this breaks in production', ['Three hundred forty KB sitewide JS dropped to twenty-eight KB when only search and theme toggle hydrated.', '**When:** When web islands partial hydration sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web islands partial hydration as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web islands partial hydration: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-404-page-product-sites": [('Why this breaks in production', ['Production web performance 404 page product sites rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance 404 page product sites sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance 404 page product sites as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance 404 page product sites: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-attribution-reporting-api": [('Why this breaks in production', ['Production web performance attribution reporting api rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance attribution reporting api sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance attribution reporting api as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance attribution reporting api: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-back-forward-cache": [('Why this breaks in production', ['Removing beforeunload handlers moved bfcache success from twelve to seventy-eight percent on Chrome Android.', '**When:** When web performance back forward cache sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance back forward cache as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance back forward cache: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-breadcrumb-navigation-seo": [('Why this breaks in production', ['Production web performance breadcrumb navigation seo rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance breadcrumb navigation seo sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance breadcrumb navigation seo as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance breadcrumb navigation seo: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-brotli-gzip-compression": [('Why this breaks in production', ['Production web performance brotli gzip compression rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance brotli gzip compression sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance brotli gzip compression as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance brotli gzip compression: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-bundle-splitting": [('Why this breaks in production', ['Eight hundred ninety KB main bundle became two hundred ten KB after route-based dynamic imports.', '**When:** When web performance bundle splitting sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance bundle splitting as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance bundle splitting: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-captcha-alternatives-ux": [('Why this breaks in production', ['Production web performance captcha alternatives ux rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance captcha alternatives ux sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance captcha alternatives ux as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance captcha alternatives ux: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-code-splitting-granularity": [('Why this breaks in production', ['Production web performance code splitting granularity rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance code splitting granularity sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance code splitting granularity as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance code splitting granularity: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-command-palette-keyboard": [('Why this breaks in production', ['Production web performance command palette keyboard rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance command palette keyboard sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance command palette keyboard as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance command palette keyboard: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-composite-layers-gpu": [('Why this breaks in production', ['Production web performance composite layers gpu rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance composite layers gpu sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance composite layers gpu as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance composite layers gpu: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-core-web-vitals": [('Why this breaks in production', ['Search Console flagged forty percent of product pages on LCP until preload, font-display swap, and banner space fixes.', '**When:** When web performance core web vitals sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance core web vitals as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance core web vitals: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-data-table-virtualization": [('Why this breaks in production', ['Production web performance data table virtualization rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance data table virtualization sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance data table virtualization as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance data table virtualization: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-debounce-throttle-input": [('Why this breaks in production', ['Production web performance debounce throttle input rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance debounce throttle input sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance debounce throttle input as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance debounce throttle input: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-document-visibility-api": [('Why this breaks in production', ['Production web performance document visibility api rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance document visibility api sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance document visibility api as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance document visibility api: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-early-hints-103": [('Why this breaks in production', ['Production web performance early hints 103 rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance early hints 103 sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance early hints 103 as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance early hints 103: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-element-timing-lcp": [('Why this breaks in production', ['Production web performance element timing lcp rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance element timing lcp sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance element timing lcp as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance element timing lcp: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-empty-state-design": [('Why this breaks in production', ['Production web performance empty state design rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance empty state design sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance empty state design as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance empty state design: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-error-recovery-retry-ui": [('Why this breaks in production', ['Production web performance error recovery retry ui rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance error recovery retry ui sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance error recovery retry ui as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance error recovery retry ui: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-filter-chip-interfaces": [('Why this breaks in production', ['Production web performance filter chip interfaces rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance filter chip interfaces sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance filter chip interfaces as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance filter chip interfaces: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-font-loading": [('Why this breaks in production', ['Web fonts blocked rendering 1.1 seconds until self-hosting, preload, and metric-matched fallbacks.', '**When:** When web performance font loading sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance font loading as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance font loading: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-http2-multiplexing-assets": [('Why this breaks in production', ['Production web performance http2 multiplexing assets rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance http2 multiplexing assets sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance http2 multiplexing assets as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance http2 multiplexing assets: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-http3-quic-benefits": [('Why this breaks in production', ['Production web performance http3 quic benefits rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance http3 quic benefits sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance http3 quic benefits as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance http3 quic benefits: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-image-formats-avif": [('Why this breaks in production', ['Hero AVIF cut transfer forty-two percent versus JPEG with no visible quality loss at equivalent settings.', '**When:** When web performance image formats avif sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance image formats avif as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance image formats avif: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-import-maps-cdn": [('Why this breaks in production', ['Production web performance import maps cdn rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance import maps cdn sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance import maps cdn as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance import maps cdn: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-inline-validation-timing": [('Why this breaks in production', ['Production web performance inline validation timing rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance inline validation timing sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance inline validation timing as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance inline validation timing: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-inp-interaction": [('Why this breaks in production', ['Filter clicks took four hundred eighty ms until we virtualized the list and yielded on the main thread.', '**When:** When web performance inp interaction sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance inp interaction as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance inp interaction: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-intersection-observer-debounce": [('Why this breaks in production', ['Production web performance intersection observer debounce rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance intersection observer debounce sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance intersection observer debounce as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance intersection observer debounce: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-islands-architecture": [('Why this breaks in production', ['Production web performance islands architecture rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance islands architecture sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance islands architecture as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance islands architecture: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-layout-shift-debugging": [('Why this breaks in production', ['Production web performance layout shift debugging rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance layout shift debugging sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance layout shift debugging as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance layout shift debugging: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-lcp-optimization": [('Why this breaks in production', ['LCP was 4.2s—TTFB and render-blocking CSS delayed hero discovery more than image bytes.', '**When:** When web performance lcp optimization sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance lcp optimization as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance lcp optimization: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-loading-state-hierarchy": [('Why this breaks in production', ['Production web performance loading state hierarchy rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance loading state hierarchy sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance loading state hierarchy as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance loading state hierarchy: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-long-tasks-monitoring": [('Why this breaks in production', ['Production web performance long tasks monitoring rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance long tasks monitoring sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance long tasks monitoring as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance long tasks monitoring: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-main-thread-work": [('Why this breaks in production', ['Production web performance main thread work rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance main thread work sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance main thread work as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance main thread work: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-maintenance-mode-page": [('Why this breaks in production', ['Production web performance maintenance mode page rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance maintenance mode page sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance maintenance mode page as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance maintenance mode page: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
  "web-performance-memory-leak-detection": [('Why this breaks in production', ['Production web performance memory leak detection rarely fails on the happy path—it fails under retries, peak traffic, and messy real devices.', '**When:** When web performance memory leak detection sits on a revenue, compliance, or Core Web Vitals critical path.', '**Avoid:** Treating web performance memory leak detection as a one-time migration without owners, metrics, or rollback.']), ('How it works', ['Map the full pipeline for web performance memory leak detection: inputs, dependencies, failure modes, and what users perceive when each step stalls.', 'Measure on mid-tier Android over throttled 4G—not only desktop lab runs.', 'Prefer platform primitives and standards before custom frameworks when they meet requirements.', 'Document which metric you expect to move (LCP, INP, CLS, recall@k, conversion) before merging.']), ('Implementation', ['Ship the smallest vertical slice on one route or tenant with observability wired first.', 'Compare canary p75 to control for a full business day in target regions.', 'Exercise refresh, back navigation, double-submit, offline, and keyboard-only paths manually.', 'Type boundaries at integration points—malformed payloads should fail in development, not checkout.']), ('Failure modes', ['Empty caches and synthetic lab tests hide CDN and auth-state bugs—warm caches and test logged-in flows.', 'Third-party scripts and tag managers change without your deploy—audit quarterly.', 'Global averages hide mobile and regional regressions—slice dashboards by device class.', 'Optimizing demo metrics while CrUX stays flat means you fixed the wrong bottleneck.']), ('Measurement', ['Leading indicators: error rate, p75 latency, validation failures, cache hit ratio.', 'Lagging indicators: support tickets, conversion, audit findings, invoice surprises.', 'Alert on week-over-week p75 regression for tier-1 surfaces before CrUX reflects it.']), ('Ship checklist', ['Name invariant, owner, leading metric, and rollback path before production promote.', 'Link runbook from the dashboard—not a wiki no one opens during incidents.', 'Re-verify quarterly after browser releases, dependency upgrades, and traffic mix shifts.'])],
}