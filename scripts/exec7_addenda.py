# auto-generated exec7 addenda
ADDENDA = {'nextjs-caching-revalidation': {'replace': 'A content editor published a pricing change; support tickets followed because three cache layers never invalidated.\n\n## Four cache layers\n\nRequest memoization deduplicates fetch within one render pass—it does not persist across users. Data Cache stores HTTP responses. Full Route Cache stores prerendered HTML. Router Cache stores client RSC payloads for instant navigations. Misattributing stale data to the wrong layer wastes hours of router.refresh() calls.\n\nMap symptoms: stale after client nav points to Router Cache; stale after hard refresh points to Data or Full Route Cache. Production validation requires next build && next start—dev mode disables most caching.\n\n## Fetch and tags\n\nTag fetches with domain events: product:123 not /products/123. One tag can invalidate list pages, detail pages, and API routes. Combine next.revalidate TTL with tags—TTL is safety net, tags are event-driven invalidation.\n\nunstable_cache wraps direct database access with the same tag semantics as fetch.\n\n## On-demand invalidation\n\nProtect revalidate webhooks with secrets. Log every invalidation with tag and source IP. Self-hosted multi-instance deployments need shared cache or explicit CDN purge alongside revalidateTag.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-csp-headers-middleware': {'replace': 'Static CSP in next.config broke Stripe and analytics. Middleware generates per-request nonces threaded through Server Components.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-csp-headers-middleware`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-csp-headers-middleware`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-csp-headers-middleware`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-csp-headers-middleware`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-csp-headers-middleware`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-csp-headers-middleware`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-draft-mode-preview-content': {'replace': 'CMS preview showed drafts but static cache served Tuesday HTML until rebuild. Draft Mode bypasses cache for editors only.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-draft-mode-preview-content`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-draft-mode-preview-content`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-draft-mode-preview-content`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-draft-mode-preview-content`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-draft-mode-preview-content`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-draft-mode-preview-content`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-dynamic-import-ssr-false': {'replace': 'Chart library accessed window during SSR. next/dynamic with ssr:false defers to client with loading skeleton matching final dimensions.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-dynamic-import-ssr-false`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-dynamic-import-ssr-false`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-dynamic-import-ssr-false`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-dynamic-import-ssr-false`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-dynamic-import-ssr-false`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-dynamic-import-ssr-false`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-edge-runtime-limitations': {'replace': 'Prisma and fs fail on Edge Runtime. Use Web Crypto, fetch, and HTTP data APIs; keep heavy logic on Node handlers.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-edge-runtime-limitations`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-edge-runtime-limitations`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-edge-runtime-limitations`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-edge-runtime-limitations`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-edge-runtime-limitations`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-edge-runtime-limitations`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-fetch-cache-next-revalidate': {'replace': 'Per-fetch next.revalidate lets listings refresh every 60s while legal copy stays daily—no single page-level TTL for everything.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-fetch-cache-next-revalidate`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-fetch-cache-next-revalidate`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-fetch-cache-next-revalidate`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-fetch-cache-next-revalidate`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-fetch-cache-next-revalidate`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-fetch-cache-next-revalidate`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-font-optimization-self-hosted': {'replace': 'Google Fonts leaked visitor IPs. next/font/local self-hosts WOFF2 with size-adjust fallbacks eliminating CLS.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-font-optimization-self-hosted`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-font-optimization-self-hosted`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-font-optimization-self-hosted`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-font-optimization-self-hosted`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-font-optimization-self-hosted`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-font-optimization-self-hosted`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-generate-static-params-dynamic': {'replace': 'Ten thousand SKUs need generateStaticParams with fallback blocking for new products between builds.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-generate-static-params-dynamic`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-generate-static-params-dynamic`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-generate-static-params-dynamic`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-generate-static-params-dynamic`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-generate-static-params-dynamic`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-generate-static-params-dynamic`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-image-optimization': {'replace': 'LCP at 4.2s from 3.2MB PNG on mobile. next/image srcsets, AVIF, priority, and sizes matching CSS layout fix it.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-image-optimization`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-image-optimization`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-image-optimization`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-image-optimization`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-image-optimization`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-image-optimization`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-instrumentation-observability': {'replace': 'instrumentation.ts registers OpenTelemetry before the server accepts traffic—trace App Router requests end-to-end.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-instrumentation-observability`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-instrumentation-observability`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-instrumentation-observability`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-instrumentation-observability`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-instrumentation-observability`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-instrumentation-observability`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-intercepting-routes-patterns': {'replace': '(.) intercepting routes open product modals over the list; hard refresh still deep-links to full page.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-intercepting-routes-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-intercepting-routes-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-intercepting-routes-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-intercepting-routes-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-intercepting-routes-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-intercepting-routes-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-internationalization-routing': {'replace': 'Locale prefix /en /de with middleware negotiates language from cookie and Accept-Language header.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-internationalization-routing`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-internationalization-routing`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-internationalization-routing`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-internationalization-routing`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-internationalization-routing`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-internationalization-routing`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-layout-shared-state-patterns': {'replace': 'React context does not cross Server Components—use URL state, cookies, or small client islands for shared UI state.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-layout-shared-state-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-layout-shared-state-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-layout-shared-state-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-layout-shared-state-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-layout-shared-state-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-layout-shared-state-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-link-prefetch-behavior': {'replace': 'Link prefetches RSC payload on viewport entry. prefetch={false} on admin routes prevents caching authenticated responses.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-link-prefetch-behavior`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-link-prefetch-behavior`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-link-prefetch-behavior`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-link-prefetch-behavior`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-link-prefetch-behavior`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-link-prefetch-behavior`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-loading-ui-error-boundaries': {'replace': 'loading.tsx streams skeletons; error.tsx catches segment errors without crashing the root layout.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-loading-ui-error-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-loading-ui-error-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-loading-ui-error-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-loading-ui-error-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-loading-ui-error-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-loading-ui-error-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-metadata-dynamic-og-images': {'replace': 'ImageResponse generates OG images from JSX for dynamic share previews when CMS has no static asset.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-metadata-dynamic-og-images`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-metadata-dynamic-og-images`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-metadata-dynamic-og-images`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-metadata-dynamic-og-images`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-metadata-dynamic-og-images`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-metadata-dynamic-og-images`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-metadata-seo-api': {'replace': 'generateMetadata merges layout and page metadata with typed Open Graph and canonical URLs.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-metadata-seo-api`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-metadata-seo-api`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-metadata-seo-api`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-metadata-seo-api`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-metadata-seo-api`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-metadata-seo-api`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-middleware-edge-runtime': {'replace': 'Middleware on Edge handles geo redirects, JWT gates, and header rewrites before render.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-middleware-edge-runtime`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-middleware-edge-runtime`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-middleware-edge-runtime`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-middleware-edge-runtime`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-middleware-edge-runtime`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-middleware-edge-runtime`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-parallel-routes-modal-patterns': {'replace': '@modal parallel slot renders intercepting modals alongside main children in layout.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-parallel-routes-modal-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-parallel-routes-modal-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-parallel-routes-modal-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-parallel-routes-modal-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-parallel-routes-modal-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-parallel-routes-modal-patterns`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-partial-prerendering-ppr': {'replace': 'PPR prerenderes static shell at build, streams dynamic inventory hole at request inside Suspense.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-partial-prerendering-ppr`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-partial-prerendering-ppr`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-partial-prerendering-ppr`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-partial-prerendering-ppr`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-partial-prerendering-ppr`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-partial-prerendering-ppr`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-route-handlers-api-design': {'replace': 'Route Handlers export HTTP verbs, validate with Zod, return Response—replace Pages API routes.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-route-handlers-api-design`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-route-handlers-api-design`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-route-handlers-api-design`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-route-handlers-api-design`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-route-handlers-api-design`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-route-handlers-api-design`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-route-segment-config-cache': {'replace': 'export const dynamic, revalidate, fetchCache control segment caching—layout cookies() force dynamic subtree.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-route-segment-config-cache`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-route-segment-config-cache`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-route-segment-config-cache`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-route-segment-config-cache`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-route-segment-config-cache`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-route-segment-config-cache`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-script-component-strategies': {'replace': 'afterInteractive vs lazyOnload trades INP against analytics completeness for third-party scripts.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-script-component-strategies`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-script-component-strategies`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-script-component-strategies`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-script-component-strategies`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-script-component-strategies`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-script-component-strategies`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-server-actions-error-handling': {'replace': 'useFormState returns field errors from actions; useFormStatus shows pending UI without full-page errors.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-server-actions-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-server-actions-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-server-actions-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-server-actions-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-server-actions-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-server-actions-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-streaming-skeleton-architecture': {'replace': 'Suspense boundaries stream slow data; skeleton dimensions must match final content to prevent CLS.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-streaming-skeleton-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-streaming-skeleton-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-streaming-skeleton-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-streaming-skeleton-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-streaming-skeleton-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-streaming-skeleton-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-turbopack-production-migration': {'replace': 'Turbopack accelerates dev builds—evaluate production bundler readiness per Next.js release notes.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-turbopack-production-migration`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-turbopack-production-migration`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-turbopack-production-migration`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-turbopack-production-migration`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-turbopack-production-migration`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-turbopack-production-migration`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'nextjs-unstable-cache-server-functions': {'replace': 'unstable_cache tags direct DB queries for revalidateTag parity with fetch cache.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `nextjs-unstable-cache-server-functions`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `nextjs-unstable-cache-server-functions`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `nextjs-unstable-cache-server-functions`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `nextjs-unstable-cache-server-functions`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `nextjs-unstable-cache-server-functions`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `nextjs-unstable-cache-server-functions`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-bullmq-job-priority-retries': {'replace': 'VIP emails queued behind CSV exports—separate queues by SLA, priority, and idempotent handlers.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-bullmq-job-priority-retries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-bullmq-job-priority-retries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-bullmq-job-priority-retries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-bullmq-job-priority-retries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-bullmq-job-priority-retries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-bullmq-job-priority-retries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-cluster-mode-vs-worker-threads': {'replace': 'cluster.fork duplicates memory; worker_threads share memory for CPU pools on one machine.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-cluster-mode-vs-worker-threads`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-cluster-mode-vs-worker-threads`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-cluster-mode-vs-worker-threads`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-cluster-mode-vs-worker-threads`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-cluster-mode-vs-worker-threads`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-cluster-mode-vs-worker-threads`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-cluster-scaling': {'replace': 'Node cluster module spreads workers across cores; master respawns crashed workers.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-cluster-scaling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-cluster-scaling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-cluster-scaling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-cluster-scaling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-cluster-scaling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-cluster-scaling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-drizzle-orm-type-safe-sql': {'replace': 'Drizzle schema-in-TypeScript with SQL-like queries—migrations and zero runtime overhead.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-drizzle-orm-type-safe-sql`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-drizzle-orm-type-safe-sql`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-drizzle-orm-type-safe-sql`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-drizzle-orm-type-safe-sql`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-drizzle-orm-type-safe-sql`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-drizzle-orm-type-safe-sql`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-env-validation-zod-envalid': {'replace': 'Validate process.env at boot with Zod—fail fast on missing DATABASE_URL not at first query.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-env-validation-zod-envalid`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-env-validation-zod-envalid`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-env-validation-zod-envalid`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-env-validation-zod-envalid`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-env-validation-zod-envalid`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-env-validation-zod-envalid`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-event-loop-lag-monitoring': {'replace': 'eventLoopUtilization and blocked-at detect synchronous work starving I/O callbacks.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-event-loop-lag-monitoring`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-event-loop-lag-monitoring`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-event-loop-lag-monitoring`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-event-loop-lag-monitoring`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-event-loop-lag-monitoring`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-event-loop-lag-monitoring`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-express-async-error-handling': {'replace': 'Express 4 misses async rejections—asyncHandler or express-async-errors forwards to error middleware.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-express-async-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-express-async-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-express-async-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-express-async-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-express-async-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-express-async-error-handling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-fastify-plugin-architecture': {'replace': 'Fastify plugins encapsulate routes with async register and explicit dependencies.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-fastify-plugin-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-fastify-plugin-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-fastify-plugin-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-fastify-plugin-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-fastify-plugin-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-fastify-plugin-architecture`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-graceful-shutdown-sigterm': {'replace': 'SIGTERM during K8s rollout: fail readiness, server.close(), drain workers within grace period.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-graceful-shutdown-sigterm`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-graceful-shutdown-sigterm`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-graceful-shutdown-sigterm`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-graceful-shutdown-sigterm`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-graceful-shutdown-sigterm`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-graceful-shutdown-sigterm`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-http-agent-keepalive-pooling': {'replace': 'http.Agent keepAlive reuses TCP to upstream APIs—critical at thousands of RPS.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-http-agent-keepalive-pooling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-http-agent-keepalive-pooling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-http-agent-keepalive-pooling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-http-agent-keepalive-pooling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-http-agent-keepalive-pooling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-http-agent-keepalive-pooling`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-memory-leak-heap-snapshot': {'replace': 'Heap snapshot diff across deploy versions finds retained closures and unclosed listeners.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-memory-leak-heap-snapshot`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-memory-leak-heap-snapshot`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-memory-leak-heap-snapshot`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-memory-leak-heap-snapshot`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-memory-leak-heap-snapshot`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-memory-leak-heap-snapshot`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-nestjs-module-boundaries': {'replace': 'Feature modules export providers; avoid circular imports—domain modules stay isolated.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-nestjs-module-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-nestjs-module-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-nestjs-module-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-nestjs-module-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-nestjs-module-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-nestjs-module-boundaries`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-opentelemetry-auto-instrumentation': {'replace': '@opentelemetry/auto-instrumentations-node captures HTTP and DB spans with minimal code.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-opentelemetry-auto-instrumentation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-opentelemetry-auto-instrumentation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-opentelemetry-auto-instrumentation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-opentelemetry-auto-instrumentation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-opentelemetry-auto-instrumentation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-opentelemetry-auto-instrumentation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-pino-structured-logging': {'replace': 'Pino JSON logs with requestId—grep by tenant without freezing SSH on unparsed strings.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-pino-structured-logging`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-pino-structured-logging`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-pino-structured-logging`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-pino-structured-logging`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-pino-structured-logging`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-pino-structured-logging`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-prisma-transaction-isolation': {'replace': 'Pick isolation level matching invariant—Serializable for ledger, Read Committed for analytics.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-prisma-transaction-isolation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-prisma-transaction-isolation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-prisma-transaction-isolation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-prisma-transaction-isolation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-prisma-transaction-isolation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-prisma-transaction-isolation`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-streams-backpressure': {'replace': 'pipe highWaterMark and pause/resume prevent memory blowup when consumer is slower than producer.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-streams-backpressure`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-streams-backpressure`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-streams-backpressure`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-streams-backpressure`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-streams-backpressure`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-streams-backpressure`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-typeorm-migration-production': {'replace': 'Run TypeORM migrations in CI with lock_timeout—never manual prod DDL without rollback script.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-typeorm-migration-production`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-typeorm-migration-production`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-typeorm-migration-production`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-typeorm-migration-production`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-typeorm-migration-production`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-typeorm-migration-production`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'node-worker-threads-cpu': {'replace': 'worker_threads for bcrypt and image resize—never block the event loop on CPU work.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `node-worker-threads-cpu`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `node-worker-threads-cpu`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `node-worker-threads-cpu`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `node-worker-threads-cpu`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `node-worker-threads-cpu`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `node-worker-threads-cpu`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'oauth-pkce-mobile': {'replace': 'PKCE replaces client secret in mobile apps—verifier never leaves device session.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `oauth-pkce-mobile`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `oauth-pkce-mobile`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `oauth-pkce-mobile`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `oauth-pkce-mobile`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `oauth-pkce-mobile`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `oauth-pkce-mobile`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'oauth2-authorization-code-flow': {'replace': 'Authorization code with PKCE—tokens via server exchange, never implicit flow or localStorage.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `oauth2-authorization-code-flow`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `oauth2-authorization-code-flow`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `oauth2-authorization-code-flow`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `oauth2-authorization-code-flow`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `oauth2-authorization-code-flow`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `oauth2-authorization-code-flow`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'oauth2-client-credentials-m2m': {'replace': 'M2M clients exchange client_id and secret for service tokens without user context.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `oauth2-client-credentials-m2m`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `oauth2-client-credentials-m2m`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `oauth2-client-credentials-m2m`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `oauth2-client-credentials-m2m`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `oauth2-client-credentials-m2m`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `oauth2-client-credentials-m2m`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'oauth2-client-credentials-scopes': {'replace': 'Scope strings limit M2M blast radius—billing.read not billing.admin.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `oauth2-client-credentials-scopes`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `oauth2-client-credentials-scopes`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `oauth2-client-credentials-scopes`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `oauth2-client-credentials-scopes`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `oauth2-client-credentials-scopes`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `oauth2-client-credentials-scopes`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'oauth2-device-authorization-tv': {'replace': 'RFC 8628 device authorization for TVs and CLI—user code on second device.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `oauth2-device-authorization-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `oauth2-device-authorization-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `oauth2-device-authorization-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `oauth2-device-authorization-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `oauth2-device-authorization-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `oauth2-device-authorization-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}, 'oauth2-device-flow-tv': {'replace': 'Netflix-style TV login: display code, poll token endpoint, respect slow_down.\n\n## How it works in production\n\nDesign for tail latency and failure—not demo happy paths alone. For `oauth2-device-flow-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Implementation patterns\n\nStart smallest measurable slice with rollback and observability before broad rollout. For `oauth2-device-flow-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Failure modes\n\nMissing idempotency, retry storms, and pool exhaustion masquerading as slow queries. For `oauth2-device-flow-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Observability\n\nMetrics on user-visible latency and error rate; logs with trace_id; traces per dependency. For `oauth2-device-flow-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Security\n\nLeast privilege credentials, no secrets in logs, fail closed on auth. For `oauth2-device-flow-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Rollout checklist\n\nFeature flags, canary, load test at 2x peak, documented rollback path. For `oauth2-device-flow-tv`, document invariants and which layer enforces them—gateway, service, or database. Operators need symptom, dashboard link, mitigation, and rollback on one runbook page. Measure p50/p95 latency, error rate, and a business metric before and after changes.\n\n## Resources\n\n- Official documentation\n- Internal runbooks and ADRs'}}

FAQS = {'nextjs-caching-revalidation': [('Why does my Next.js page show stale data after a database update?', "The App Router caches at multiple layers: fetch responses, full route HTML, and client router cache. A database update alone does not invalidate any of them. Call revalidatePath or revalidateTag after mutations, or set cache: 'no-store' on fetches that must always be fresh."), ('What is the difference between revalidatePath and revalidateTag?', "revalidatePath invalidates cached pages tied to a URL path. revalidateTag invalidates fetch requests tagged with next: { tags: ['posts'] }. Use tags when one data source feeds multiple pages."), ('How do I disable caching for a specific page?', "Export const dynamic = 'force-dynamic' in the page or layout, or use cache: 'no-store' on all fetch calls in that route segment. Use sparingly—disabling cache removes static generation benefits.")], 'nextjs-csp-headers-middleware': [('What is CSP Headers via Next.js Middleware?', 'CSP Headers via Next.js Middleware is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt CSP Headers via Next.js Middleware?', 'Adopt CSP Headers via Next.js Middleware when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with CSP Headers via Next.js Middleware?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-draft-mode-preview-content': [('What is Draft Mode and Preview Content Workflows?', 'Draft Mode and Preview Content Workflows is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Draft Mode and Preview Content Workflows?', 'Adopt Draft Mode and Preview Content Workflows when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Draft Mode and Preview Content Workflows?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-dynamic-import-ssr-false': [('What is dynamic() with ssr?', 'dynamic() with ssr is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt dynamic() with ssr?', 'Adopt dynamic() with ssr when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with dynamic() with ssr?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-edge-runtime-limitations': [('What is Edge Runtime Limitations and Workarounds?', 'Edge Runtime Limitations and Workarounds is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Edge Runtime Limitations and Workarounds?', 'Adopt Edge Runtime Limitations and Workarounds when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Edge Runtime Limitations and Workarounds?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-fetch-cache-next-revalidate': [('What is fetch Cache and next.revalidate Patterns?', 'fetch Cache and next.revalidate Patterns is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt fetch Cache and next.revalidate Patterns?', 'Adopt fetch Cache and next.revalidate Patterns when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with fetch Cache and next.revalidate Patterns?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-font-optimization-self-hosted': [('What is Self-Hosted Font Optimization in Next.js?', 'Self-Hosted Font Optimization in Next.js is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Self-Hosted Font Optimization in Next.js?', 'Adopt Self-Hosted Font Optimization in Next.js when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Self-Hosted Font Optimization in Next.js?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-generate-static-params-dynamic': [('What is generateStaticParams for Dynamic Routes?', 'generateStaticParams for Dynamic Routes is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt generateStaticParams for Dynamic Routes?', 'Adopt generateStaticParams for Dynamic Routes when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with generateStaticParams for Dynamic Routes?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-image-optimization': [('Should I use next/image or a regular img tag?', 'Use next/image for any content image where you control dimensions or aspect ratio. It provides automatic srcset generation, lazy loading, format negotiation (WebP/AVIF), and CLS prevention via reserved space. Use img only for user-uploaded images with unknown dimensions where you cannot set width and height.'), ('How do I optimize images hosted on an external CDN?', "Add the domain to images.remotePatterns in next.config.js. Next.js will proxy and transform external images through its optimization API. For high-traffic sites, use a custom loader pointing to your CDN's image transformation service."), ('Why is my LCP image loading slowly?', 'LCP images need priority={true} to skip lazy loading and preload early. Set explicit width and height to prevent layout shift. Serve appropriately sized images—an 800px display does not need a 4000px source file.')], 'nextjs-instrumentation-observability': [('What is Next.js Instrumentation for Web Observability?', 'Next.js Instrumentation for Web Observability is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Next.js Instrumentation for Web Observability?', 'Adopt Next.js Instrumentation for Web Observability when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Next.js Instrumentation for Web Observability?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-intercepting-routes-patterns': [('What is Intercepting Routes for In-Place Navigation?', 'Intercepting Routes for In-Place Navigation is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Intercepting Routes for In-Place Navigation?', 'Adopt Intercepting Routes for In-Place Navigation when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Intercepting Routes for In-Place Navigation?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-internationalization-routing': [('What is Internationalization Routing in App Router?', 'Internationalization Routing in App Router is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Internationalization Routing in App Router?', 'Adopt Internationalization Routing in App Router when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Internationalization Routing in App Router?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-layout-shared-state-patterns': [('What is Shared State Patterns Across Layouts?', 'Shared State Patterns Across Layouts is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Shared State Patterns Across Layouts?', 'Adopt Shared State Patterns Across Layouts when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Shared State Patterns Across Layouts?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-link-prefetch-behavior': [('What is Link Prefetch Behavior and Control?', 'Link Prefetch Behavior and Control is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Link Prefetch Behavior and Control?', 'Adopt Link Prefetch Behavior and Control when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Link Prefetch Behavior and Control?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-loading-ui-error-boundaries': [('What is loading.tsx and Error Boundary Architecture?', 'loading.tsx and Error Boundary Architecture is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt loading.tsx and Error Boundary Architecture?', 'Adopt loading.tsx and Error Boundary Architecture when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with loading.tsx and Error Boundary Architecture?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-metadata-dynamic-og-images': [('What is Dynamic OG Image Generation?', 'Dynamic OG Image Generation is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Dynamic OG Image Generation?', 'Adopt Dynamic OG Image Generation when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Dynamic OG Image Generation?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-metadata-seo-api': [('How do I set page-specific title and description in the App Router?', 'Export a metadata object or generateMetadata function from page.tsx or layout.tsx. Static pages use export const metadata = { title, description }. Dynamic pages use export async function generateMetadata({ params }) to fetch data and return metadata per route.'), ('Does Next.js Metadata API replace next/head from Pages Router?', 'Yes. The Metadata API in App Router replaces next/head. It generates head tags at build time or request time with type safety. You cannot use next/head in App Router components.'), ('How do I add JSON-LD structured data?', "Return a script tag with type application/ld+json from your page component or layout. Alternatively, include it in the metadata.other field. Validate with Google's Rich Results Test before deploying.")], 'nextjs-middleware-edge-runtime': [('What can Next.js middleware do that route handlers cannot?', 'Middleware runs before a request reaches any route handler or page. It can rewrite URLs, redirect, modify request/response headers, and set cookies globally. Use it for auth gates, geo routing, and bot detection that must apply across many routes without duplicating logic.'), ('Why is my middleware not running on API routes?', 'Check your matcher config. The default matcher excludes _next/static, _next/image, and favicon.ico. If you explicitly set matcher, ensure /api/:path* is included. Middleware only runs on paths matching the matcher pattern.'), ('What are Edge Runtime limitations in middleware?', 'Middleware runs on the Edge Runtime, which lacks Node.js APIs (fs, child_process, native modules). Use Web Crypto, fetch, and URL APIs. Database connections must go through HTTP-based clients or edge-compatible drivers like @vercel/postgres or Upstash Redis.')], 'nextjs-parallel-routes-modal-patterns': [('What is Parallel Routes for Modal and Drawer Patterns?', 'Parallel Routes for Modal and Drawer Patterns is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Parallel Routes for Modal and Drawer Patterns?', 'Adopt Parallel Routes for Modal and Drawer Patterns when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Parallel Routes for Modal and Drawer Patterns?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-partial-prerendering-ppr': [('What is Next.js Partial Prerendering in Production?', 'Next.js Partial Prerendering in Production is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Next.js Partial Prerendering in Production?', 'Adopt Next.js Partial Prerendering in Production when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Next.js Partial Prerendering in Production?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-route-handlers-api-design': [('What is Route Handlers API Design Patterns?', 'Route Handlers API Design Patterns is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Route Handlers API Design Patterns?', 'Adopt Route Handlers API Design Patterns when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Route Handlers API Design Patterns?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-route-segment-config-cache': [('What is Route Segment Config and Cache Control?', 'Route Segment Config and Cache Control is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Route Segment Config and Cache Control?', 'Adopt Route Segment Config and Cache Control when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Route Segment Config and Cache Control?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-script-component-strategies': [('What is next/script Loading Strategies?', 'next/script Loading Strategies is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt next/script Loading Strategies?', 'Adopt next/script Loading Strategies when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with next/script Loading Strategies?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-server-actions-error-handling': [('What is Server Actions Error Handling Patterns?', 'Server Actions Error Handling Patterns is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Server Actions Error Handling Patterns?', 'Adopt Server Actions Error Handling Patterns when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Server Actions Error Handling Patterns?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-streaming-skeleton-architecture': [('What is Streaming Skeleton Architecture in Next.js?', 'Streaming Skeleton Architecture in Next.js is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Streaming Skeleton Architecture in Next.js?', 'Adopt Streaming Skeleton Architecture in Next.js when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Streaming Skeleton Architecture in Next.js?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-turbopack-production-migration': [('What is Migrating to Turbopack in Production?', 'Migrating to Turbopack in Production is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt Migrating to Turbopack in Production?', 'Adopt Migrating to Turbopack in Production when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with Migrating to Turbopack in Production?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'nextjs-unstable-cache-server-functions': [('What is unstable_cache for Server Functions?', 'unstable_cache for Server Functions is a production pattern for frontend and product engineering teams building performant, accessible web applications. It addresses real constraints around user experience, security, and measurable outcomes — not theoretical best practices disconnected from shipping code.'), ('When should teams adopt unstable_cache for Server Functions?', 'Adopt unstable_cache for Server Functions when you have field data or user research showing pain — slow interactions, accessibility gaps, conversion drop-offs, or security findings — and simpler fixes have been exhausted. Pilot on one route or feature before rolling out platform-wide.'), ('What are common mistakes with unstable_cache for Server Functions?', 'Teams often optimize for demo metrics instead of field data, skip accessibility validation, or roll out without rollback paths. Measure before and after with RUM, run axe checks in CI, and feature-flag risky changes so you can revert without redeploying.')], 'node-bullmq-job-priority-retries': [('What breaks if node bullmq job priority retries is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node bullmq job priority retries?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node bullmq job priority retries?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-cluster-mode-vs-worker-threads': [('What breaks if node cluster mode vs worker threads is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node cluster mode vs worker threads?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node cluster mode vs worker threads?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-cluster-scaling': [('Does the cluster module make a single Node.js request faster?', 'No. Cluster spreads concurrent requests across CPU cores—each worker still handles one request at a time on its event loop. A single CPU-bound request on one worker is not faster. Throughput scales with core count for I/O-bound workloads.'), ('When should I use cluster vs running multiple containers?', 'Use cluster for single-server deployments where you want to utilize all cores without orchestration overhead. Use multiple containers (Kubernetes, Docker Compose) when you need horizontal scaling across machines, independent deploys, or isolation between instances.'), ('How do I share sessions across cluster workers?', 'In-memory sessions break with cluster—each worker has its own heap. Store sessions in Redis, Memcached, or a database. The same applies to in-memory caches and rate limit counters.')], 'node-drizzle-orm-type-safe-sql': [('What breaks if node drizzle orm type safe sql is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node drizzle orm type safe sql?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node drizzle orm type safe sql?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-env-validation-zod-envalid': [('What breaks if node env validation zod envalid is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node env validation zod envalid?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node env validation zod envalid?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-event-loop-lag-monitoring': [('What breaks if node event loop lag monitoring is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node event loop lag monitoring?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node event loop lag monitoring?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-express-async-error-handling': [('What breaks if node express async error handling is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node express async error handling?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node express async error handling?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-fastify-plugin-architecture': [('What breaks if node fastify plugin architecture is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node fastify plugin architecture?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node fastify plugin architecture?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-graceful-shutdown-sigterm': [('What breaks if node graceful shutdown sigterm is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node graceful shutdown sigterm?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node graceful shutdown sigterm?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-http-agent-keepalive-pooling': [('What breaks if node http agent keepalive pooling is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node http agent keepalive pooling?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node http agent keepalive pooling?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-memory-leak-heap-snapshot': [('What breaks if node memory leak heap snapshot is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node memory leak heap snapshot?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node memory leak heap snapshot?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-nestjs-module-boundaries': [('What breaks if node nestjs module boundaries is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node nestjs module boundaries?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node nestjs module boundaries?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-opentelemetry-auto-instrumentation': [('What breaks if node opentelemetry auto instrumentation is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node opentelemetry auto instrumentation?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node opentelemetry auto instrumentation?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-pino-structured-logging': [('What breaks if node pino structured logging is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node pino structured logging?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node pino structured logging?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-prisma-transaction-isolation': [('What breaks if node prisma transaction isolation is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node prisma transaction isolation?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node prisma transaction isolation?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-streams-backpressure': [('When should I use streams instead of reading a file into a buffer?', 'Use streams when data size is unknown or exceeds available memory—a 2 GB log file, HTTP response body, or database cursor. Buffering loads everything into RAM. Streams process chunks incrementally, keeping memory usage flat regardless of file size.'), ('What happens if I ignore backpressure?', 'The producer outruns the consumer. Chunks accumulate in internal buffers, memory grows until the process crashes with OOM. Symptoms include rising heap usage during file uploads and event loop lag. Always pipe streams or handle drain events.'), ('Are Node.js streams still relevant with async/await?', 'Yes. Async/await simplifies control flow but does not replace streaming I/O. Use stream/promises helpers (pipeline, finished) to combine streams with async error handling. For HTTP proxying and ETL, streams remain the correct abstraction.')], 'node-typeorm-migration-production': [('What breaks if node typeorm migration production is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test node typeorm migration production?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer node typeorm migration production?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'node-worker-threads-cpu': [('When should I use worker threads vs child processes?', 'Use worker threads for CPU-bound tasks that need shared memory or frequent communication—image resizing, JSON parsing of large payloads, bcrypt hashing. Use child processes for isolation (crash one without killing the server), running non-Node code, or when memory leaks in untrusted code are a concern.'), ('How many worker threads should I create?', 'Match CPU core count for CPU-bound work—creating more threads than cores adds context-switching overhead without throughput gain. For mixed I/O and CPU workloads, cores minus one (leaving one for the main event loop) is a reasonable starting point.'), ('Can worker threads access the same variables as the main thread?', 'Not directly—they have separate V8 isolates. Share data via message passing (postMessage), SharedArrayBuffer for typed arrays, or transfer ArrayBuffers with zero-copy. Treat workers like separate programs that exchange messages.')], 'oauth-pkce-mobile': [('What is OAuth 2.0 PKCE?', "PKCE (Proof Key for Code Exchange, pronounced 'pixy') is an extension to the OAuth 2.0 authorization code flow that protects public clients like mobile apps from authorization code interception. The client generates a random secret (code_verifier), sends a hashed version (code_challenge) when starting the flow, and proves it holds the original verifier when exchanging the code for tokens. An attacker who steals the authorization code can't use it without the verifier."), ("Why can't mobile apps just use a client secret?", "A mobile app is a public client — its binary is distributed to every user and can be decompiled, so any embedded client secret is effectively public. Because you can't keep a secret in an app, the traditional confidential-client protection doesn't apply. PKCE replaces the static client secret with a per-request dynamic secret that never has to be stored in the app, which is exactly why it's mandatory for mobile."), ('Is the implicit flow still acceptable for mobile?', 'No. The implicit flow returned tokens directly in the redirect, which exposed them to interception and left no way to prove client legitimacy. OAuth 2.0 Security Best Current Practice and OAuth 2.1 formally deprecate it. The authorization code flow with PKCE is now the required approach for mobile and single-page apps; there is no good reason to use implicit flow in a new app.')], 'oauth2-authorization-code-flow': [('Why is PKCE required for public clients?', 'Public clients (SPAs, mobile apps) cannot store a client secret securely. PKCE replaces the secret with a per-request code verifier and challenge. Without PKCE, an attacker who intercepts the authorization code can exchange it for tokens at the token endpoint.'), ('What is the difference between authorization code and implicit flow?', 'The implicit flow returned tokens directly in the URL fragment—exposing them to browser history and referrer headers. It is deprecated. Authorization code flow with PKCE exchanges a short-lived code server-side for tokens, keeping access tokens out of the browser URL.'), ('Where should I store tokens in a SPA?', 'Store refresh tokens in HttpOnly, Secure, SameSite cookies set by your backend. Keep access tokens in memory (JavaScript variable) for API calls. Never store tokens in localStorage—any XSS vulnerability exposes them.')], 'oauth2-client-credentials-m2m': [('When should I use client credentials instead of API keys?', 'Use client credentials when you need scoped access (read-only vs write), token expiration with automatic renewal, audit trails tied to client identity, or integration with an existing OAuth infrastructure. API keys are simpler for single-purpose internal tools with one permission level.'), ('Can client credentials flow access user data?', 'No. Client credentials grants access on behalf of the application itself, not a user. The token represents the service account. To act on behalf of a user, use authorization code flow or token exchange (RFC 8693).'), ('How do I rotate client secrets without downtime?', 'Issue a new client secret while the old one remains valid for an overlap period (7–14 days). Deploy the new secret to all consuming services, verify traffic succeeds, then revoke the old secret. Automate rotation quarterly.')], 'oauth2-client-credentials-scopes': [('What breaks if oauth2 client credentials scopes is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test oauth2 client credentials scopes?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer oauth2 client credentials scopes?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'oauth2-device-authorization-tv': [('What breaks if oauth2 device authorization tv is misconfigured?', 'User-visible errors, stale data, or security findings—often silent until audit or peak traffic.'), ('How do we test oauth2 device authorization tv?', 'Integration tests on production-like topology and load tests at 2x peak—not unit tests alone.'), ('When defer oauth2 device authorization tv?', 'Only pre-production without compliance drivers—document debt if deferred.')], 'oauth2-device-flow-tv': [('When is the device flow required instead of authorization code flow?', 'Use device flow when the client cannot receive redirects—smart TVs, streaming sticks, printers, CLI tools on headless servers, and IoT devices without browsers. The user authorizes on a separate device (phone or laptop) while the constrained device polls for tokens.'), ('How long should device codes remain valid?', 'Typical device code lifetime is 15–30 minutes. Polling interval starts at 5 seconds with exponential backoff per RFC 8628. Expire codes aggressively—stale codes sitting on a TV screen are a social engineering vector if someone walks by and uses them.'), ('Can device flow use PKCE?', 'PKCE is not part of the standard device flow—the device never handles an authorization code directly. Security relies on the user physically entering the code on a trusted secondary device. Bind tokens to device_id claims where possible.')]}
